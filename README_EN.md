# POLY MONITOR (English)

Please refer to `README.md` for setup and operations.

Key points:
1. Install with virtualenv and `pip install -e .`
2. Use `tmux` for long-running tasks
3. `run` is for testing (console output), `start` is for production (log-first)
